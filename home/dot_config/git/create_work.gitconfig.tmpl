# WORK identity, applied under ~/workspace/intella/ (see ~/.gitconfig).
# Created once by chezmoi; edit freely, `chezmoi apply` will not overwrite it.
[user]
	name = {{ .work.name }}
	email = {{ .work.email }}
# SSH keys are selected per-host by ~/.ssh/config, not here.
